These are the my dotfiles. To use them clone this repo and run:

    ln -sf <dir_to_this_file>/.* /home/$USER/

You will also want to have a full color console so run: 

    sudo apt-get install ncurses-term
    echo export TERM=xterm-256color >> ~/.bashrc
    source ~/.bashrc
